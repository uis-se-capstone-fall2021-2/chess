package game;

public class PlayerRecord extends PlayerInfo {

    // how a Player is represented in persistence
    public PlayerRecord(int playerId, String alias) {
        super(playerId, alias);
        //TODO: what does this class need?
    }
    
}
